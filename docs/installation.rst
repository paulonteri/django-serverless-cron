============
Installation
============

At the command line::

    $ easy_install django-serverless-cron

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-serverless-cron
    $ pip install django-serverless-cron
